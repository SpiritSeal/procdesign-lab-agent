[project]
name = "lab-agent"
version = "0.1.0"
description = "LLM agent that completes CS3220 Lab #2 (Branch Prediction)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=1.0.0",
]
